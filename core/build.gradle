apply plugin: "java"

dependencies {
    implementation 'org.jetbrains:annotations:20.1.0'
}

sourceCompatibility = 1.8
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]

tasks.processResources.dependsOn(':packTextures')